# Дневник АД

PWA приложение для отслеживания артериального давления на Android. Стильное и практичное приложение для ежедневного мониторинга АД утром и вечером.

## Возможности

- 📝 Ввод данных АД утром и вечером (систолическое, диастолическое, пульс, заметки)
- 💾 Локальное хранение данных (IndexedDB) - данные остаются на устройстве
- 📊 Интерактивный график изменений за последние 30 дней
- 📋 История всех записей с цветовой индикацией (нормальное/повышенное/пониженное)
- 🎨 Современный дизайн с объемными серыми карточками на белом фоне
- 📱 Полная поддержка PWA (можно установить на домашний экран Android)
- 🔄 Автоматическое обновление приложения
- 📱 Оптимизировано для мобильных устройств

## Установка

```bash
npm install
```

## Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## Установка на Android

1. Откройте приложение в браузере Chrome на Android
2. Нажмите меню (три точки) → "Добавить на главный экран"
3. Приложение будет установлено как PWA

## Технологии

- **React 18** + **TypeScript** - современный UI
- **Vite** - быстрая сборка
- **IndexedDB** - надежное локальное хранение данных
- **Recharts** - красивые графики
- **Vite PWA Plugin** - автоматическая настройка PWA
- **date-fns** - работа с датами
- **Design Tokens** - единая система дизайна

## Структура проекта

```
src/
  ├── components/        # React компоненты
  │   ├── BloodPressureForm.tsx  # Форма ввода АД
  │   ├── RecordsList.tsx          # Список записей
  │   └── Chart.tsx                # График
  ├── lib/
  │   ├── db.ts                    # IndexedDB работа с данными
  │   └── design-tokens.ts         # Дизайн-токены
  └── styles/
      └── global.css               # Глобальные стили
```

## Особенности

- **Автоматическое определение времени суток** - форма предлагает "Утро" или "Вечер" в зависимости от текущего времени
- **Цветовая индикация** - автоматическое определение нормального/повышенного/пониженного давления
- **Офлайн работа** - все данные хранятся локально, приложение работает без интернета
- **Адаптивный дизайн** - оптимизировано для экранов Android

